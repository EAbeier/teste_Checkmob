import 'package:flutter/material.dart';

class AddQuestionaire extends StatelessWidget {
  const AddQuestionaire({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Novo questionário"));
  }
}
